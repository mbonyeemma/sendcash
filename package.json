{
  "name": "sendicash-frontends",
  "private": true,
  "version": "1.0.0",
  "description": "SendiCash frontends — admin panel workspace (user web app lives in ./web as a standalone project).",
  "workspaces": [
    "admin"
  ],
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev:web": "npm run dev --prefix web",
    "dev:admin": "npm run dev --workspace admin",
    "build:web": "npm run build --prefix web",
    "build:admin": "npm run build --workspace admin",
    "build": "npm run build:web && npm run build:admin",
    "lint:admin": "npm run lint --workspace admin"
  }
}
